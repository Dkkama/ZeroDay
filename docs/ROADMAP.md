# Roadmap

Preliminary: local/Cloud Run clerk console, zip + IMAP ingest, Cursor test path, Vertex demo path, human review, exports.

Final-round extras:

- Vertex as the default production path with a proper job queue
- Gmail / Microsoft 365 OAuth instead of IMAP password
- Firestore + Cloud Storage instead of a local JSON file
- OCR / vision for image-only bills (the contest key still wants escalate on those scans)
- Clerk SSO and role-based audit
